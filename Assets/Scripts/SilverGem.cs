using System.Collections; using System.Collections.Generic; using UnityEngine; using UnityEngine.UI;  public class SilverGem : MonoBehaviour {      public GameObject scoreBox;     public AudioSource collectSound;      void OnTriggerEnter()     {         GlobalScore.currentScore += 100;         collectSound.Play();         Destroy(gameObject); // gameObject is a reference to the objet that the script is attached,          // Destroy basically destroys the object passed as a parameter     } } 